name: Atualizar Dashboard Histórico

on:
  # Opção 1: Agendamento Automático
  schedule:
    # Roda no dia 1 de cada mês, às 02:00 BRT (05:00 UTC)
    - cron: '0 5 1 * *' 
  
  # Opção 2: Execução Manual
  # Permite que você acione o workflow manualmente pela interface do GitHub Actions
  workflow_dispatch:
    inputs:
      motivo:
        description: 'Motivo da execução manual (Opcional)'
        required: false
        default: 'Teste ou correção de dados históricos'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' # Mantenha a versão que você usa

      - name: Instalar Dependências
        run: |
          pip install pandas gspread json # Garante que todas as libs estejam instaladas

      - name: Executar o Agente Histórico
        # Injeção do Secret, garantindo a autenticação (Consistência com Agente Diário)
        env:
          GCP_SA_CREDENTIALS: ${{ secrets.GCP_SA_CREDENTIALS }}
        run: |
          python analise_historica.py

      - name: Commit e Push do Dashboard
        # Este passo é crucial para que o novo HTML vá para o GitHub Pages
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Atualização automática do Dashboard Histórico (Dia 1 do mês)"
          files: dashboard_historico.html # O arquivo que o agente Python gera

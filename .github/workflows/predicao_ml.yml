name: Gerar Previsão de Vendas (Machine Learning) 🔮

on:
  # 1. ATIVAÇÃO MANUAL (Para testes e validação sob demanda)
  workflow_dispatch:
    inputs:
      motivo:
        description: 'Motivo da execução manual (e.g., Baseline Naive, Validação de Dados)'
        required: false
        default: 'Ativação manual para Modelo Naive.'
  
  # 2. ATIVAÇÃO AGENDADA (Roda no dia 1 de cada mês)
  schedule:
    - cron: '0 4 1 * *' 

jobs:
  build_ml:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write # Permite que o bot comite o novo dashboard

    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # <<<<< PASSO CORRIGIDO PARA INCLUIR TODAS AS DEPENDÊNCIAS >>>>>
      - name: Instalar Dependências (ML + GEMINI)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt 
      # <<<<< FIM DA CORREÇÃO >>>>>

      - name: Executar o Modelo de Previsão Sênior
        env:
          GCP_SA_CREDENTIALS: ${{ secrets.GCP_SA_CREDENTIALS }}
          # Não esqueça do Segredo do Gemini (Se já estiver no seu .yml, ótimo!)
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }} 
        run: |
          python predicao_ml.py

      - name: Commit e Push do Dashboard de ML
        # Mensagem atualizada para refletir a nova capacidade Sênior
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Atualização automática do Dashboard de Previsão ML/IA (Análise Sênior Gemini)"
          commit_body: "Motivo da Execução: ${{ github.event.inputs.motivo || 'Execução agendada/padrão.' }}"
          file_pattern: dashboard_ml_insights.html

name: Dashboard de Vendas - Automação Diária

# 1. QUANDO RODAR
on:
  schedule:
    # Roda às 9:00 UTC (6h do Brasil) e 21:00 UTC (18h do Brasil)
    - cron: '0 9,21 * * *'
  
  # 🚨 CORREÇÃO: ADICIONE ESTE BLOCO PARA O BOTÃO MANUAL
  workflow_dispatch: 
  
  # Se quiser rodar a cada push no main, adicione o bloco abaixo
  # push:
  #   branches:
  #     - main 

# 2. O QUE FAZER
jobs:
  run_analysis:
    # 🚨 ESTAS SÃO AS LINHAS CRÍTICAS PARA CORRIGIR O ERRO 403
    permissions:
      contents: write 
    
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout do Código
        uses: actions/checkout@v4
        
      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: ⚙️ Instalar Dependências
        run: |
          pip install pandas plotly gspread
          
      - name: 🏃 Rodar Análise de Vendas
        env:
          GCP_SA_CREDENTIALS: ${{ secrets.GCP_SA_CREDENTIALS }}
        run: python analise_vendas_automacao.py
        
      # 2.5. Commit e Push do novo dashboard HTML
      - name: 💾 Commit do Dashboard Atualizado
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'ci: 📈 Dashboard de vendas atualizado via automação'
          # CORRIGE O 'Warning: Unexpected input(s) files'
          file_pattern: 'dashboard_vendas_final.html' 
          skip_dirty_check: false
          
      - name: ✅ Sucesso!
        run: echo "Análise e commit finalizados!"
